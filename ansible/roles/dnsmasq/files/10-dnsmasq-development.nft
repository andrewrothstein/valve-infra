#!/usr/bin/nft -f

# note: development rules / config meant to supplement 10-dnsmasq.nft
table inet filter {
    # "acceptable" domains to IPs
    set allowed_addresses {
        type ipv4_addr
        flags interval
        elements = {
            46.23.90.166,       # tchar server
        }
    }
}
