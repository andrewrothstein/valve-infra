[Unit]
PartOf=infra.service
# NOTE: *not* adding an explicit dependency on executor... even though the
# gitlab runner config is generated by executor, the gitlab runner will hot
# reload it on change.

[Service]
EnvironmentFile={{ base_config_env_file }}
EnvironmentFile=-{{ config_env_file }}

[Install]
WantedBy=infra.service
